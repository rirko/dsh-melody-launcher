# CONTEXT — DSH Launcher 领域词汇表

本文件只收录**领域术语**及其精确含义，不含实现细节。术语在对话中定下来就立即写入。

## 核心对象

- **DSH（DeepSeek Harness）**：本启动器托管的本地 AI 工作台运行时，本体通过可执行命令启动一个 Web 工作台。
- **启动器（Launcher）**：管理 DSH 的桌面程序本体（即本项目）。同一代码库内同时服务两类用户：C 端小白与开发者。

## 环境与隔离

- **DSH 版本**：本机可并行安装的多个 DSH 运行时版本。启动器全局选中某一个版本用于启动；版本与版本之间天然隔离。
- **配置文件（Profile）**：一个独立的 DSH 环境，持有自己的一套插件启用清单与依赖。DSH 用哪个配置文件启动，就加载哪些插件。配置文件之间互不影响，是"与其他环境隔离"的真正载体。
- **一级导航（顶栏 tab）**：常驻顶栏把 C 端管理能力拍平成同级页面：启动、DSH版本、插件、技能、预设、整合包；点「开发人员选项」进入管理界面（二级，顶栏仍在，点任意 tab 即返回）。同一固定尺寸窗口内换页，不再有"进入/返回设置页"的层级跳转。
- **启动页（启动 tab）**：一级导航的默认页——左品牌列（logo / DSH-Melody-Launcher / 版本 / QQ群），右下方启动大按钮（状态写进按钮文案）、运行时出现「打开网页」小方块、其下「版本选择（整合包）」跳转。
- **管理界面（surface）**：开发人员选项进入的完整工作区，承载所有管理功能（资源市场、DSH Market、GitHub、运行环境、旧设置面板等）。

## 可独立管理的资源（各有各的目录）

- **插件（Plugin）**：以 DSH 加载层形式生效的组件，随 Profile 装入；开关决定是否在启用清单里。
- **技能（Skill）**：DSH 的全局技能资源，独立目录，可整体开关。
- **Agent 预设（Preset）**：DSH 的预设形配置资源，独立目录，可整体开关。
- **应用加载项（Application Addon）**：可作为 DSH 替代 Web 托管方的独立应用，独立目录，可整体开关。

## 整合包

- **整合包（Pack）**：可整体导出/导入的成套内容，声明所需的 DSH 版本，并包含插件、技能、Agent 预设与应用加载项的清单（部分含离线本体）。导入生成独立 Profile，与现有环境隔离。
- **导出 / 导入**：把当前环境（或某个整合包）的成套内容打成压缩包带走 / 从压缩包恢复为一套新环境。

## 模型与指令

- **模型（Model / 提供商）**：可在配置文件中登记的多家 API 提供商与模型（如 sensenova、opencode、deepseek 等），用于 Agent 对话与工具调用。
- **权限（Permission）**：DSH 对操作的授权策略（如默认预设）。

## 界面分层（2026-08 新决策）

- **C 端设置**：面向普通用户的简洁管理能力（版本 / 插件 / 技能 / 预设 / 整合包，全部可对照"各自文件夹"）。原为独立全屏二级页，现已拍平为顶栏一级导航 tab；「启动设置」对话框（DSH 目录、端口、主题等启动器自身设置）入口在开发人员选项内。
- **开发者模式**：面向开发者的完整管理能力（资源市场、DSH Market、GitHub、运行环境、旧设置面板等）。
- **MCP**：尚未成型的对象——本机 DSH 当前没有 MCP 目录或配置，本阶段不做；将来 DSH 支持时再以"带开关的独立目录资源"形态加入 C 端设置。